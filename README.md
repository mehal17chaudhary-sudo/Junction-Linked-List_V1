# Junction-Linked-List_V1
JLI: a linked list with an externally-indexed search layer instead of per-node skip-list pointers. Same O(log n) search, 20–25% less index memory, proven via 41 structural invariants and 87 benchmark configs against a skip list.
